type HeaderProps = {
    title: string
    description?: string
    className?: string
}

interface MenuItem {
    title: string;
    icon: LucideIcon;
    href: string;
}

interface Window {
    google?: unknown;
}
